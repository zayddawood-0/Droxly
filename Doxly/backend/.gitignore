.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# env files — .env.example is committed, every other .env* variant is not
.env*
!.env.example

# R2 — LocalFilesystemStorageProvider's dev/test upload target
# (core/storage.py); never committed, same category as any other local
# runtime data.
.local-storage/
